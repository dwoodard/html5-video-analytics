/*
it should 
	be
*/

QUnit.test( "hello test", function( assert ) {
  assert.ok( 1 == "1", "Passed!" );
});

test( "hello test", function( assert ) {
  assert.ok( 1 == "1", "Passed!" );
});

test( "hello test", function( assert ) {
  assert.ok( 1 == "1", "Passed!" );
});